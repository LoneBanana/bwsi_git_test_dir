This repo was primarily used for learning git/github using the MIT/BWSI training course.

Though this repository has no files, it has primarily been used to explore the ".git" file and try messing around with hooks and variouus other utilities/plumbing commands used by git to get the job done.